# Local overrides for `wrangler dev`. Committed on purpose: there is nothing
# secret here, and without it a fresh clone cannot run the relay or its tests.
#
# The deployed relay only admits the deployed game. A relay on your own machine
# has to admit whatever is on your own machine — a dev server on some port, and
# the test client, which sends no Origin at all.
ALLOWED_ORIGINS=""
